#include <iostream>
#include <string>
using namespace std;
int N,M;
int arr[10];
string str[N];

void f(int k) {}

int main() {
ios::sync_with_stdio(0);
cin.tie(0);

cin >> N >> M;
for(int i = 0; i < N; i++)
cin >> str[N];


f(0);
}